server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yrodda/Shopping-Cart-POC.git
          search-paths: ConfigInfo
  application:
    name: configuration-service